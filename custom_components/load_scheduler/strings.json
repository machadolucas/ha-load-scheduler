{
  "config": {
    "step": {
      "user": {
        "title": "Load Scheduler",
        "description": "Set up the hub that all scheduled loads will share. Point it at the sensor that publishes electricity prices with a forecast for at least the next day. You can add the sell-price and solar-production forecasts now or later; loads (water heater, dishwasher, EV, floor heating, …) are added afterwards from the integration's page.",
        "data": {
          "name": "Name",
          "buy_price_entity": "Buy-price forecast sensor",
          "sell_price_entity": "Sell-price forecast sensor (optional)",
          "solar_forecast_entity": "Solar-production forecast sensor(s) (optional)",
          "consumption_baseline_w": "Baseline household consumption (W)"
        },
        "data_description": {
          "buy_price_entity": "A sensor exposing today/tomorrow price slots in its attributes (e.g. Nord Pool, ENTSO-e, or a day-ahead template). Used to find the cheapest run times.",
          "sell_price_entity": "What you earn exporting 1 kWh. Enables buy-vs-sell solar arbitration: running on solar costs the foregone sell price.",
          "solar_forecast_entity": "Per-slot PV forecast sensor(s) (e.g. Solcast 'today' + 'tomorrow', whose 'detailedForecast' is read). Enables valuing slots with solar excess at the sell price.",
          "consumption_baseline_w": "Always-on house load subtracted from the PV forecast to estimate exportable excess. A flat estimate for now (derived from statistics in a later release)."
        }
      },
      "reconfigure": {
        "title": "Load Scheduler",
        "description": "Update the shared price and solar forecast sources.",
        "data": {
          "name": "Name",
          "buy_price_entity": "Buy-price forecast sensor",
          "sell_price_entity": "Sell-price forecast sensor (optional)",
          "solar_forecast_entity": "Solar-production forecast sensor(s) (optional)",
          "consumption_baseline_w": "Baseline household consumption (W)"
        }
      }
    },
    "error": {
      "invalid_price_entity": "That sensor doesn't expose a recognised price forecast (no today/tomorrow price list found in its attributes)."
    },
    "abort": {
      "already_configured": "This price sensor is already configured.",
      "reconfigure_successful": "Sources updated."
    }
  },
  "config_subentries": {
    "load": {
      "initiate_flow": {
        "user": "Add load"
      },
      "entry_type": "Load",
      "step": {
        "init": {
          "title": "Add a load",
          "description": "Configure how this load is scheduled into cheap hours. Leave the window empty to search the next 24 hours; set an earliest start and a deadline (e.g. 21:00 → 07:00) to constrain it.",
          "data": {
            "name": "Name",
            "mode": "Scheduling mode",
            "target_minutes": "Target runtime",
            "earliest": "Earliest start (optional)",
            "deadline": "Deadline (optional)",
            "runs_per_day": "Runs per day (sequential)",
            "min_separation_minutes": "Minimum separation between runs (optional)",
            "min_service_minutes": "Minimum service per day (optional)",
            "price_cap": "Maximum price (optional, €/kWh)",
            "controlled_entity": "Switch to control (optional)",
            "allow_solar": "Value solar excess for this load",
            "draw_kw": "Load power draw (optional, kW)"
          },
          "data_description": {
            "mode": "Non-sequential = the cheapest scattered slots (pausable loads). Sequential = the cheapest contiguous block(s). Informational = compute and display only, never switch anything.",
            "target_minutes": "How long the load should run, in minutes.",
            "runs_per_day": "For sequential loads, how many separate blocks to schedule (e.g. run a washing machine twice).",
            "min_service_minutes": "A guaranteed minimum that runs even when prices are above the cap, so the load never starves.",
            "price_cap": "Discretionary runtime above the minimum is only scheduled at or below this price.",
            "controlled_entity": "A switch / input_boolean the integration turns on and off with the schedule. Leave empty for informational loads.",
            "allow_solar": "When the hub has a solar forecast, value slots with predicted excess at the sell price instead of the buy price.",
            "draw_kw": "The load's power, used to value how much of a slot's solar excess it can actually absorb. Without it, any excess counts as fully solar."
          }
        }
      }
    }
  },
  "selector": {
    "mode": {
      "options": {
        "non_sequential": "Non-sequential (scattered, pausable)",
        "sequential": "Sequential (contiguous block)",
        "informational": "Informational (display only)"
      }
    }
  },
  "entity": {
    "binary_sensor": {
      "running": {
        "name": "Running"
      }
    },
    "sensor": {
      "schedule": {
        "name": "Schedule"
      }
    },
    "number": {
      "target": {
        "name": "Target runtime"
      }
    },
    "switch": {
      "enabled": {
        "name": "Enabled"
      }
    },
    "button": {
      "boost": {
        "name": "Boost"
      }
    },
    "calendar": {
      "schedule": {
        "name": "Schedule"
      }
    }
  }
}
