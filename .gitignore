# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/
coverage.xml
*.egg-info/
.mypy_cache/

# Node / frontend card build
node_modules/
custom_components/load_scheduler/frontend/dist/*.map

# Editors / OS
.DS_Store
.idea/
.vscode/

# Temporary: pushing these needs a GitHub token with `workflow` scope (or an
# SSH key). The files exist locally; remove this block and commit them once
# that's configured. See README/CLAUDE notes.
/.github/workflows/
